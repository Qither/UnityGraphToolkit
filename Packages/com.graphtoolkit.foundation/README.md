# Graph Foundation

Provides deterministic node auto-layout through the
`GraphNodeLayoutExpansion` API for Unity 6.

`GraphProcessor` is supplied separately by Unity Package Manager from the upstream
[`alelievr/NodeGraphProcessor`](https://github.com/alelievr/NodeGraphProcessor) repository.
