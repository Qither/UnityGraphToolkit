# NPBehave

Contains the existing code-first NPBehave runtime plus the visual graph authoring workflow. Export and runtime loading share the same explicit type allowlist through `NPBehaveJsonSerializer`.

